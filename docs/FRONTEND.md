# Frontend — Retablo

Angular 22 standalone, zoneless, signals. Pure CSS tokens «Carmín de Telón».

Rutas auth (`/login`, `/entrar`, `/registro`) antes del shell. Público: `/`, `/obras`, `/obras/:id`, `/funciones/:id`, `/entrada/:code`. Auth: checkout, confirmación, mis-entradas, cuenta. Staff/admin: `/staff`, `/staff/scan`, `/admin`.

QR con `bypassSecurityTrustHtml`. 409 humano junto a `#pay-action`. «desde» = `fromPriceCents`. Figcaption HTML, no texto recortado en SVG.
